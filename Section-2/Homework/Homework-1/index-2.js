console.log(`What are the differences between let and var?`);
console.log(`var and let are both used for variable declaration in javascript but`);
console.log(`the difference between them is that var is function scoped and let is block scoped`);
console.log(`It can be said that a variable declared with var is `);
console.log(`defined throughout the program as compared to let`)